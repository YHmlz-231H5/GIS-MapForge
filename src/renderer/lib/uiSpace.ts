/**
 * Shared UI spacing tokens (CSS vars --ui-space / --ui-space-sm).
 * Large gutters → 10px, tight gutters → 6px.
 */
export const UI_SPACE = 10;
export const UI_SPACE_SM = 10;

/** Panel edge tab width (w-7) */
export const PANEL_TAB_W = 28;
